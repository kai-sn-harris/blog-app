<%- include("./partials/header", {title: "Edit"}) %>
    <%- include("./partials/nav", {active: ""}) %>

    <h1 class="text-center">helo</h1>

    <div class="row-fluid d-flex justify-content-center">
        <div class="col col-lg-6 col-md-8 col-sm-12">
            <form action="" method="post">
                <div class="form-group">
                    <label for="title">Title:</label>
                    <input class="form-control" type="text" id="title" name="title" value="<%= post.title %>">
                </div>
        
                <div class="form-group">
                    <label for="body">Body:</label>
                    <textarea rows="10" class="form-control" id="body" name="body"><%= post.body %></textarea>
                </div>

                <button class="btn btn-lg btn-success">Update</button>
            </form>
        </div>
    </div>

<%- include("./partials/footer") %>